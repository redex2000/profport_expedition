- page_title "Список галактик"
.float-right
  = link_to 'Создать', new_galaxy_path, class: 'btn btn-info'
%table.table.table-hover
  %thead
    %tr
      %th Название

  %tbody
    - @galaxies.each do |galaxy|
      %tr
        %td= link_to galaxy.title, galaxy_path(galaxy)
